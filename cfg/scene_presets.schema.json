{
    "$jsonSchema": {
        "bsonType": "object",
        "required": [ "name","lights","audio" ],
        "properties": {
            "name": {
                "bsonType": "string",
                "description": "The name of the scene"
            },
            "lights": {
                "bsonType" : "object",
                "description": "Key value of lights and their preset name"
            },
            "audio": {
                "bsonType": "string",
                "description": "Audio to play in the in envrionemnt"
            }
        }
    }
 }